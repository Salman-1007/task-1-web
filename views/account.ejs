<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Account</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/checkout.css">
    <style>
        .account-hero {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 3rem 0;
            text-align: center;
        }
        
        .account-card {
            border: 1px solid #dee2e6;
            border-radius: 8px;
            margin-bottom: 2rem;
        }
        
        .account-header {
            background-color: #f8f9fa;
            border-bottom: 1px solid #dee2e6;
            padding: 1.5rem;
            border-radius: 7px 7px 0 0;
        }
        
        .account-body {
            padding: 2rem;
        }
        
        .info-box {
            background-color: #f8f9fa;
            padding: 1.5rem;
            border-radius: 8px;
            border-left: 4px solid #0d6efd;
            margin-bottom: 1rem;
        }
        
        .info-label {
            font-weight: 600;
            color: #6c757d;
            font-size: 0.85rem;
            text-transform: uppercase;
            margin-bottom: 0.5rem;
        }
        
        .info-value {
            font-size: 1.1rem;
            font-weight: 500;
        }
        
        .quick-links {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 1.5rem;
            margin-top: 1.5rem;
        }
        
        .quick-link-btn {
            padding: 1rem;
            text-align: center;
            border-radius: 8px;
            text-decoration: none;
            border: 2px solid #dee2e6;
            transition: all 0.3s;
            color: inherit;
        }
        
        .quick-link-btn:hover {
            border-color: #0d6efd;
            box-shadow: 0 4px 12px rgba(13, 110, 253, 0.15);
            transform: translateY(-2px);
        }
        
        .quick-link-btn i {
            font-size: 2rem;
            color: #0d6efd;
            display: block;
            margin-bottom: 0.5rem;
        }
        
        .logout-btn {
            margin-top: 2rem;
            text-align: center;
        }
        
        .auth-links {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 1.5rem;
            margin-top: 1rem;
        }
        
        .auth-card {
            border: 2px solid #dee2e6;
            border-radius: 8px;
            padding: 1.5rem;
            text-align: center;
            transition: all 0.3s;
            text-decoration: none;
            color: inherit;
        }
        
        .auth-card:hover {
            border-color: #0d6efd;
            box-shadow: 0 4px 12px rgba(13, 110, 253, 0.15);
        }
        
        .auth-card i {
            font-size: 2.5rem;
            color: #0d6efd;
            margin-bottom: 0.5rem;
            display: block;
        }
        
        .auth-card h5 {
            font-weight: 700;
            margin-bottom: 0.5rem;
        }
        
        .auth-card p {
            color: #6c757d;
            font-size: 0.9rem;
            margin: 0;
        }
    </style>
</head>

<body>
    <%- include('partials/header') %>

        <div class="account-hero">
            <div class="container">
                <h1 class="mb-2">
                    <% if (isLoggedIn) { %>
                        Welcome,
                        <%= user.fullName || user.username %>!
                            <% } else { %>
                                My Account
                                <% } %>
                </h1>
                <p class="mb-0">Manage your account and settings</p>
            </div>
        </div>

        <div class="container py-5">
            <% if (isLoggedIn) { %>
                <!-- Logged In View -->
                <div class="row">
                    <div class="col-lg-10 mx-auto">
                        <!-- Profile Card -->
                        <div class="account-card">
                            <div class="account-header">
                                <i class="fas fa-user-circle me-2"></i>Your Profile
                            </div>
                            <div class="account-body">
                                <div class="row">
                                    <div class="col-md-6">
                                        <div class="info-box">
                                            <div class="info-label">Username</div>
                                            <div class="info-value">
                                                <%= user.username %>
                                            </div>
                                        </div>
                                    </div>
                                    <div class="col-md-6">
                                        <div class="info-box">
                                            <div class="info-label">Email</div>
                                            <div class="info-value">
                                                <%= user.email %>
                                            </div>
                                        </div>
                                    </div>
                                    <div class="col-md-6">
                                        <div class="info-box">
                                            <div class="info-label">Full Name</div>
                                            <div class="info-value">
                                                <%= user.fullName || 'Not set' %>
                                            </div>
                                        </div>
                                    </div>
                                    <div class="col-md-6">
                                        <div class="info-box">
                                            <div class="info-label">Account Type</div>
                                            <div class="info-value">
                                                <% if (user.isAdmin) { %>
                                                    <span class="badge bg-danger">Administrator</span>
                                                    <% } else { %>
                                                        <span class="badge bg-info">Customer</span>
                                                        <% } %>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>

                        <!-- Customer Only Links -->
                        <% if (!user.isAdmin) { %>
                            <div class="account-card">
                                <div class="account-header">
                                    <i class="fas fa-shopping-bag me-2"></i>Shopping
                                </div>
                                <div class="account-body">
                                    <div class="quick-links">
                                        <a href="/orders" class="quick-link-btn">
                                            <i class="fas fa-receipt"></i>
                                            <span>My Orders</span>
                                        </a>
                                        <a href="/products" class="quick-link-btn">
                                            <i class="fas fa-store"></i>
                                            <span>Shop Products</span>
                                        </a>
                                        <a href="/cart" class="quick-link-btn">
                                            <i class="fas fa-shopping-cart"></i>
                                            <span>My Cart</span>
                                        </a>
                                    </div>
                                </div>
                            </div>
                            <% } %>

                                <!-- Admin Only Links -->
                                <% if (user.isAdmin) { %>
                                    <div class="account-card">
                                        <div class="account-header">
                                            <i class="fas fa-crown me-2"></i>Administrator
                                        </div>
                                        <div class="account-body">
                                            <p class="text-muted mb-3">Access the admin panel to manage products, orders, and store settings.</p>
                                            <a href="/admin" class="btn btn-warning btn-lg w-100">
                                                <i class="fas fa-cog me-2"></i>Go to Admin Panel
                                            </a>
                                        </div>
                                    </div>
                                    <% } %>

                                        <!-- Logout -->
                                        <div class="logout-btn">
                                            <a href="/auth/logout" class="btn btn-danger btn-lg">
                                                <i class="fas fa-sign-out-alt me-2"></i>Logout
                                            </a>
                                        </div>
                    </div>
                </div>

                <% } else { %>
                    <!-- Not Logged In View -->
                    <div class="row">
                        <div class="col-lg-8 mx-auto">
                            <div class="account-card mb-5">
                                <div class="account-body">
                                    <h3 class="text-center mb-4">Choose Your Access</h3>
                                    <div class="auth-links">
                                        <a href="/auth/login" class="auth-card">
                                            <i class="fas fa-user"></i>
                                            <h5>Customer Login</h5>
                                            <p>Access your customer account</p>
                                        </a>
                                        <a href="/auth/signup" class="auth-card">
                                            <i class="fas fa-user-plus"></i>
                                            <h5>Customer Sign Up</h5>
                                            <p>Create a new account</p>
                                        </a>
                                    </div>
                                </div>
                            </div>

                            <!-- Admin Login -->
                            <div class="account-card">
                                <div class="account-header">
                                    <i class="fas fa-crown me-2"></i>Administrator
                                </div>
                                <div class="account-body">
                                    <p class="text-muted mb-4">If you are an administrator, login with your admin credentials.</p>
                                    <a href="/auth/admin-login" class="btn btn-warning w-100 btn-lg">
                                        <i class="fas fa-sign-in-alt me-2"></i>Admin Login
                                    </a>
                                </div>
                            </div>
                        </div>
                    </div>
                    <% } %>
        </div>

        <%- include('partials/footer') %>

            <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
            <script src="/js/main.js"></script>
</body>

</html>